#!/usr/bin/env python3
"""
edgeless-guard CLI

Subcommands:
  init      write install manifest
  validate  validate policy files
  check     audit a target
  enforce   audit a target and halt on violation in block mode
  report    render a saved report
  self-test run tests/test_edgeless_guard.py
  version   print version
"""
from __future__ import annotations

import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "lib"))
from edgeless_guard import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
