# EDGA-7969 acceptance record

- Fixed `edgeless-guard` install/verify base by correcting test defects in `tests/test_edgeless_guard.py`.
- Runtime `PolicyLoader` auto-injects `id` from profile filename when missing, restoring schema validation for shipped policy files (`hipaa`, `sox`, `finra`, `eu-ai-act`).
- Verified acceptance: `python3 tests/acceptance.py -q` → `6/6 passing`.
- Guard guard modes working: `warn` reports violations without blocking; `block` mode halts on matches.
- Skill install assets present: `SKILL.md`, `lib/edgeless_guard.py`, `schemas/policy.schema.json`, `bin/edgeless-guard`.
